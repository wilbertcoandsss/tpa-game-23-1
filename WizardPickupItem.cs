using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WizardPickupItem : MonoBehaviour
{
    public Animator _itemAnimator, _animator;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.C))
        //{
        //    Debug.Log("Masuk satu");

        //    if (ItemScript.isPicking)
        //    {
        //        Debug.Log("Masuk dua");

        //        _animator.SetBool("isPicking", true);      
        //    }

        //    else
        //    {

        //        ItemScript.isPicking = false;
        //    }
        //}
        //else
        //{
        //    _animator.SetBool("isPicking", false);
        //}
    }
}
